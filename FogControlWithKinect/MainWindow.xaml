<Window x:Class="FogControlWithKinect.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FogControlWithKinect"
        xmlns:utils="clr-namespace:FogControlWithKinect.Utils"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Fog Control With Kinect"
        MinHeight="240"
        MinWidth="320"
        Height="250"
        Width="400"
        Loaded="Window_Loaded"
        Closed="Window_Closed" Icon="/Assets/Images/icon.png">
    <Window.Resources>
        <utils:BoolInverseConverter x:Key="Inverse"/>
        
        <Style x:Key="Header" TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="Status" TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="12"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#FF444444"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0"
                    IsEnabled="{Binding IsReady}">
            <Label Style="{StaticResource Header}"
                   Content="Mouse operations"/>
            <StackPanel HorizontalAlignment="Center"
                        IsEnabled="{Binding IsRunning, Converter={StaticResource Inverse}}">
                <CheckBox IsEnabled="False"
                          Content="Move"
                          IsChecked="true"/>
                <CheckBox x:Name="chkMouseDrag"
                          Content="Click-n-Drag"
                          Tag="Touch"
                          IsChecked="True"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1"
                    IsEnabled="{Binding IsReady}">
            <Label Style="{StaticResource Header}"
                   Content="Hand"/>
            <CheckBox x:Name="chkHand"
                      Style="{StaticResource Switch}"
                      IsEnabled="{Binding IsRunning, Converter={StaticResource Inverse}}"
                      Content="Right"
                      Tag="Left"
                      IsChecked="True"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2"
                    IsEnabled="{Binding IsReady}"
                    HorizontalAlignment="Center">
            <Button Content="{Binding StartInteractionButtonText, Mode=OneWay}"
                    Width="160"
                    Margin="5"
                    Click="StartStopInteraction_Click"/>
            <Button Content="Calibrate"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource Inverse}}"
                    Width="160"
                    Margin="5" Click="Calibrate_Click"/>
        </StackPanel>

        <Label Grid.Row="2" Grid.ColumnSpan="2"
               Style="{StaticResource Status}"
               Content="{Binding StatusText}"/>
    </Grid>
</Window>
